# OpenCode Share 详细使用教程

本文档说明如何在 Windows 11 主机上启动本项目，并让同一局域网内的手机通过浏览器查看或控制主机端的 `oc` 会话。

## 1. 项目目录

本文档中的所有命令，除特别说明外，都在下面这个目录执行：

```powershell
D:\share\code\opencodeShare
```

如果你当前不在这个目录，先执行：

```powershell
cd D:\share\code\opencodeShare
```

## 2. 前置条件

需要满足：

- Windows 11 主机。
- 已安装 Node.js。
- `oc` 已经能在 PowerShell 中直接运行。
- 手机和电脑连接同一个局域网，例如同一个 Wi-Fi。

检查 Node.js：

```powershell
node -v
npm -v
```

检查 `oc`：

```powershell
oc
```

如果 `oc` 可以正常进入 opencode 交互界面，说明命令可用。

## 3. 安装依赖

进入项目目录：

```powershell
cd D:\share\code\opencodeShare
```

安装依赖：

```powershell
npm install
```

如果已经安装过，可以跳过这一步。

如果安装 `node-pty` 失败，通常是 Windows 缺少 C++ 编译环境。需要安装 Visual Studio Build Tools，并勾选 C++ 构建工具。

## 4. 创建配置文件

在项目目录执行：

```powershell
copy .env.example .env
```

然后用编辑器打开 `.env`，例如：

```powershell
notepad .env
```

默认配置类似这样：

```env
HOST=0.0.0.0
PORT=8787
VIEW_PASSWORD=readonly123
CONTROL_PASSWORD=control123
SHELL=powershell.exe
START_COMMAND=oc
TOKEN_SECRET=change-this-long-random-string
HISTORY_LIMIT=200000
```

建议修改这几项：

```env
VIEW_PASSWORD=你的只读密码
CONTROL_PASSWORD=你的控制密码
TOKEN_SECRET=一段随机长字符串
```

说明：

- `VIEW_PASSWORD`：手机输入这个密码后，只能看 `oc` 输出，不能操作。
- `CONTROL_PASSWORD`：手机输入这个密码后，可以像在主机 PowerShell 里一样操作 `oc`。
- `START_COMMAND=oc`：服务启动后会自动运行 `oc`。
- `HOST=0.0.0.0`：允许局域网内其他设备访问。
- `PORT=8787`：手机访问使用的端口。

不要把 `VIEW_PASSWORD` 和 `CONTROL_PASSWORD` 设置成一样。

## 5. 生产模式启动

推荐日常使用生产模式。

在项目目录执行构建：

```powershell
npm run build
```

然后启动服务：

```powershell
npm start
```

启动成功后，终端会显示类似：

```text
opencode-share listening on http://0.0.0.0:8787
Started 'oc' in D:\share\code\opencodeShare
```

这表示服务已经启动，并且已经自动在当前目录运行了 `oc`。

保持这个 PowerShell 窗口不要关闭。关闭窗口后，手机页面也会断开。

## 6. 查看电脑局域网 IP

打开另一个 PowerShell，执行：

```powershell
ipconfig
```

找到当前正在使用的网卡，例如 WLAN 或以太网，查看 `IPv4 地址`。

示例：

```text
IPv4 地址 . . . . . . . . . . . . : 192.168.1.23
```

那么手机访问地址就是：

```text
http://192.168.1.23:8787
```

你的 IP 会和示例不同，以你自己电脑上的 `IPv4 地址` 为准。

## 7. 手机访问

确保手机和电脑在同一个 Wi-Fi 下。

在手机浏览器打开：

```text
http://电脑IPv4地址:8787
```

例如：

```text
http://192.168.1.23:8787
```

页面会要求输入密码。

输入只读密码：

- 进入只读模式。
- 可以看到主机端 `oc` 输出。
- 不能输入内容。

输入控制密码：

- 进入控制模式。
- 可以看到主机端 `oc` 输出。
- 可以通过手机输入内容并发送给 `oc`。
- 可以使用页面上的快捷键按钮。

## 8. 控制模式使用方法

控制模式页面是手机原生 OpenCode 对话控制台，底部有多行输入框、发送按钮、换行按钮、`/ 命令` 和 `更多`。

查看历史消息：

- 页面只显示从 OpenCode 本地数据库读取的结构化聊天记录，不再显示终端 TUI 实时画面。
- 历史页会区分 `你`、`AI`、`思考` 和工具调用，避免显示 PowerShell 启动文字、进度条、方块等终端界面符号。
- 顶部会显示当前模式、模型、路径和上下文长度。
- 点击 `Sessions` 可以打开侧边栏，查看并切换当前目录下的 OpenCode 会话历史。
- 如果浏览器底部地址栏占用空间，向下滑动页面或切换横屏通常能释放更多显示区域。

普通输入：

1. 在底部多行输入框输入内容。
2. 点击 `发送`，内容会发送到主机端 `oc`，并自动追加一次回车。
3. 点击 `换行`，只会在输入框里插入换行，不会立即发送。
4. 如果使用电脑键盘访问页面，可以按 `Ctrl+Enter` 发送。

空输入时点击 `发送`，等同于向终端发送一次回车。

快捷键按钮：

- `/ 命令`：打开常用 slash command 面板，点击后会填入输入框，可编辑后发送。
- `更多`：打开操作面板，支持切换模型、切换 Session、打开编辑器、新 Session、中断和清空输入。
- `新 Session`：通过当前 `oc` 进程发送 OpenCode 的新会话快捷键，不会重启服务。

页面不再要求你在手机上操作 OpenCode 的终端菜单。常用操作已经做成手机按钮和底部面板。

## 9. 只读模式使用方法

只读模式适合把当前 `oc` 会话分享给别人看。

只读用户：

- 可以查看结构化会话历史和当前状态。
- 刷新页面后会恢复最近输出。
- 不能向主机端发送任何输入。

即使只读用户伪造前端请求，后端也会拒绝写入终端。

## 10. 开发模式启动

如果你要修改前端或后端代码，可以使用开发模式。

在项目目录执行：

```powershell
npm run dev
```

开发模式会同时启动：

- 后端服务：`http://电脑IP:8787`
- Vite 前端服务：`http://电脑IP:5173`

开发模式下，手机建议访问：

```text
http://电脑IPv4地址:5173
```

例如：

```text
http://192.168.1.23:5173
```

Vite 会自动把 `/api` 和 `/ws` 代理到后端。

## 11. 停止服务

在运行服务的 PowerShell 窗口里按：

```text
Ctrl+C
```

然后根据提示确认即可。

停止服务后：

- 后端 Web 服务关闭。
- `oc` 会话结束。
- 手机页面断开连接。

## 12. 常见问题

### 手机打不开页面

检查这几项：

- 手机和电脑是否在同一个局域网。
- 访问地址是否使用电脑的 IPv4 地址，不是 `0.0.0.0`。
- 端口是否正确，生产模式默认是 `8787`。
- Windows 防火墙是否拦截 Node.js。

可以先在电脑浏览器访问：

```text
http://127.0.0.1:8787
```

如果电脑能打开、手机打不开，通常是防火墙或局域网问题。

### Windows 防火墙拦截

可以在 Windows 安全中心允许 Node.js 通过专用网络。

也可以手动开放 TCP 端口 `8787`。

### 登录提示密码错误

检查 `.env` 中的：

```env
VIEW_PASSWORD=
CONTROL_PASSWORD=
```

修改 `.env` 后需要重启服务才会生效。

### 页面能打开，但没有看到 oc 输出

检查运行服务的 PowerShell 窗口是否有报错。

确认 `.env` 中是：

```env
START_COMMAND=oc
```

并确认在同一个 PowerShell 环境里直接执行下面命令可用：

```powershell
oc
```

### npm install 失败

如果错误和 `node-pty`、`gyp`、`msbuild` 有关，安装 Visual Studio Build Tools，并选择 C++ 构建工具。

安装后重新执行：

```powershell
npm install
```

## 13. 安全提醒

控制模式等同于把主机 PowerShell 终端交给手机操作。

请注意：

- 不要把服务暴露到公网。
- 不要做路由器端口映射。
- 控制密码要设置得足够复杂。
- 只在可信局域网内使用。
- 不使用时及时按 `Ctrl+C` 停止服务。

## 14. 最短启动流程

如果已经安装依赖并配置好 `.env`，以后每次使用只需要：

```powershell
cd D:\share\code\opencodeShare
npm run build
npm start
```

然后手机访问：

```text
http://电脑IPv4地址:8787
```
